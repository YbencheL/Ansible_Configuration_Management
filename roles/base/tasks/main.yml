- name: server setup
  ansible.builtin.shell: |
    set -e
    echo "Starting process..."
    apt update && apt upgrade -y
    apt install -y curl git fail2ban
    systemctl enable --now fail2ban
    echo "Done."
